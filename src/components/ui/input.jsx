const Input = ({ placeholder }) => {
  return (
    <input
      className="text-center rounded-md bg-black p-[10px] text-[14px]"
      placeholder={placeholder}
    ></input>
  );
};

export default Input;
